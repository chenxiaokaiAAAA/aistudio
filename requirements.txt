Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Login==0.6.3
Werkzeug==2.3.7
qrcode==7.4.2
# Pillow>=10.3.0 为 psd-tools 所需，与样机套图兼容
Pillow>=10.3.0
# 样机套图 - PSD智能对象替换
psd-tools>=1.9.0
gunicorn==21.2.0
requests==2.31.0
plyer==2.1.0
pyttsx3==2.90
# PostgreSQL驱动（迁移到PostgreSQL时使用）
psycopg2-binary>=2.9.0
# 支持.env文件加载环境变量
python-dotenv>=1.0.0
# Redis缓存支持（可选，提升性能）
redis>=5.0.0
# 阿里云OSS存储支持（图片存储到OSS时使用）
oss2>=2.18.0
# 响应压缩支持（可选，提升性能）
flask-compress>=1.13
# API 文档自动化（Swagger/OpenAPI）
flasgger>=0.9.7

# 测试框架
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-flask>=1.2.0

# 代码质量检查工具
black>=23.0.0
flake8>=6.0.0
pylint>=2.17.0
mypy>=1.0.0
isort>=5.12.0
pre-commit>=3.0.0

