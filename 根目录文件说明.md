# AI-studio 根目录文件说明

## 📋 文件分类

**最后更新**：2026-01-27

整理完成后，根目录下保留的重要文件按用途分类如下：

---

## ✅ 核心代码文件（保留在根目录）

这些是系统运行必需的核心文件，保留在根目录：

### 主服务文件
- `test_server.py` - 主服务文件（Flask应用）
- `start.py` - 开发环境启动脚本
- `start_production.py` - 生产环境启动脚本（Gunicorn）

### 核心模块
- `order_notification.py` - 订单通知模块
- `wechat_notification.py` - 微信通知模块
- `printer_client.py` - 打印系统客户端
- `local_printer_client.py` - 本地打印客户端
- `local_printer.py` - 本地打印模块

### 配置文件
- `server_config.py` - 服务器配置（环境切换，不提交到Git）
- `printer_config.py` - 打印系统配置（不提交到Git）
- `size_config.py` - 尺寸配置
- `gunicorn.conf.py` - Gunicorn配置
- `requirements.txt` - Python依赖列表

### 应用目录
- `app/` - 应用主目录
  - `app/routes/` - 路由模块
  - `app/services/` - 服务模块
  - `app/utils/` - 工具函数
  - `app/models.py` - 数据模型
- `templates/` - HTML模板
- `static/` - 静态文件（CSS、JS）
- `config/` - 配置文件目录

## 📄 重要文档（保留在根目录）

这些是日常使用的重要文档，保留在根目录方便查找：

- `README.md` - 项目主说明文档
- `根目录文件说明.md` - 本文件，项目文件结构说明
- `Linux命令快速参考.md` - Linux服务器常用命令速查表
- `README_重要文档.md` - 重要文档快速索引

---

## 📄 文档文件（已整理到 docs/）

所有文档已整理到 `docs/` 目录：

- `docs/deployment/` - 部署相关文档
- `docs/features/` - 功能说明文档
- `docs/api/` - API文档

**查看**：运行 `README_重要文档.md` 查看文档索引

---

## 🛠️ 工具脚本（已整理到 scripts/）

所有脚本已整理到 `scripts/` 目录：

- `scripts/deployment/` - 部署脚本
- `scripts/database/` - 数据库脚本
- `scripts/tools/` - 工具脚本

---

## ⚙️ 配置文件（应移动到 config/）

这些配置文件应该统一管理：

### Nginx配置
- `nginx.conf` - Nginx主配置文件
- `nginx_simple.conf` - Nginx简化配置
- `nginx_backup_20250918_215014.conf` - Nginx备份配置
- `mime.types` - MIME类型配置

### 其他配置
- `config.yml` - YAML配置文件
- `pet-painting.service` - Linux服务配置文件

**建议操作**：创建 `config/` 目录，移动这些文件

---

## 📊 数据文件（应移动到 data/）

这些是数据导出、备份、日志文件：

### JSON数据
- `database_backup_log.json` - 数据库备份日志
- `missing_users_analysis.json` - 用户分析数据
- `october_17_19_report_20251020_105019.json` - 报表数据
- `promotion_tracks_report_20251020_105530.json` - 推广追踪报表

### CSV数据
- `orders_2025-10-17_to_2025-10-19_20251020_105018.csv` - 订单导出
- `promotion_tracks_all_20251020_105530.csv` - 推广追踪数据
- `promotion_tracks_october_17_19_20251020_105530.csv` - 推广追踪数据
- `sample_users.csv` - 示例用户数据

### SQL脚本
- `database_migration.sql` - 数据库迁移脚本
- `database_migration_add_pet_images.sql` - 宠物图片迁移
- `database_migration_photo_signup.sql` - 摄影报名迁移

### 其他数据
- `sample_production_info.txt` - 示例生产信息
- `qidong.txt` - 启动配置
- `营运管理员账户说明.txt` - 账户说明

**建议操作**：创建 `data/` 目录，按类型分类存放

---

## 📝 日志文件

- `app.log` - 应用日志

**建议操作**：移动到 `logs/` 目录（如果存在）或创建 `logs/` 目录

---

## 📚 其他文件

### HTML文件
- `test_select_style.html` - 测试页面
- `debug_watermarked_response.html` - 调试页面

**建议操作**：移动到 `scripts/tests/` 或删除

### PDF文件
- `冲印系统传片接口协议说明(简版)V2.pdf` - 接口协议文档

**建议操作**：移动到 `docs/api/` 目录

### Shell脚本
- `logistics_callback_curl.sh` - curl测试脚本
- `商家测试_curl.sh` - 商家测试脚本
- `start.sh` - Linux启动脚本

**建议操作**：移动到 `scripts/tools/` 目录

### PowerShell脚本
- `auto_start_nginx.ps1` - Nginx自动启动脚本
- `logistics_callback_powershell.ps1` - PowerShell测试脚本
- `start_powershell.ps1` - PowerShell启动脚本

**建议操作**：移动到 `scripts/tools/` 目录

### 其他
- `zip_structure_example.txt` - ZIP结构示例

**建议操作**：移动到 `docs/` 或删除

---

## 🎯 建议的目录结构

```
AI-studio/
├── test_server.py          # 主服务文件（保留）
├── start.py                # 启动脚本（保留）
├── requirements.txt          # 依赖列表（保留）
├── server_config.py        # 服务器配置（保留）
├── printer_config.py       # 冲印配置（保留）
├── size_config.py          # 尺寸配置（保留）
├── franchisee_routes.py    # 加盟商路由（保留）
├── order_notification.py   # 通知模块（保留）
├── wechat_notification.py  # 微信通知（保留）
├── printer_client.py       # 冲印客户端（保留）
├── sync_config_routes.py   # 同步配置（保留）
│
├── config/                 # 配置文件目录（新建）
│   ├── nginx.conf
│   ├── nginx_simple.conf
│   ├── mime.types
│   └── config.yml
│
├── data/                   # 数据文件目录（新建）
│   ├── exports/           # 导出数据
│   ├── backups/           # 备份数据
│   └── reports/           # 报表数据
│
├── logs/                   # 日志目录（新建）
│   └── app.log
│
├── docs/                   # 文档目录（已有）
├── scripts/                # 脚本目录（已有）
└── batch/                  # 批处理目录（已有）
```

---

## 📋 整理建议

### 立即整理（不影响运行）

1. **创建新目录**：
   - `config/` - 配置文件
   - `data/` - 数据文件
   - `logs/` - 日志文件

2. **移动文件**：
   - 配置文件 → `config/`
   - 数据文件 → `data/`
   - 日志文件 → `logs/`
   - 文档文件 → `docs/features/`
   - 工具脚本 → `scripts/tools/`

3. **清理文件**：
   - 删除临时测试文件
   - 删除过期的备份文件

---

**文档版本**：v1.0  
**创建时间**：2026-01-14
