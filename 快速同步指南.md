# 快速同步指南

## 🚀 最简单的方法

### 本地（Windows）

1. **运行批处理脚本**
   ```
   完整同步到GitHub并部署.bat
   ```

2. **或者手动执行**
   ```cmd
   git add .
   git commit -m "更新代码"
   git push origin main
   ```

### 服务器（Linux）

1. **运行同步脚本（推荐）**
   ```bash
   chmod +x /root/project_code/scripts/deployment/sync_from_github.sh
   bash /root/project_code/scripts/deployment/sync_from_github.sh
   ```

2. **或者手动执行**
   ```bash
   cd /root/project_code
   git pull origin main
   systemctl restart aistudio
   ```

## 📋 完整工作流程

### 日常开发

1. **本地修改代码** → 测试
2. **提交到 Git** → `git add . && git commit -m "描述" && git push`
3. **服务器同步** → `cd /root/project_code && git pull && systemctl restart aistudio`

### 检查文件完整性

在服务器上运行：
```bash
chmod +x /root/project_code/scripts/deployment/check_files_complete.sh
bash /root/project_code/scripts/deployment/check_files_complete.sh
```

## ⚠️ 重要提示

1. **数据库文件不会自动同步**（这是正常的）
   - 需要单独备份和恢复数据库文件

2. **敏感配置文件不会同步**（这是正常的）
   - `server_config.py`、`.env` 等需要在服务器上单独配置

3. **图片文件不会同步**（这是正常的）
   - `uploads/`、`final_works/`、`hd_images/` 需要单独处理

## 📖 详细文档

查看 [代码同步完整指南.md](docs/deployment/代码同步完整指南.md) 获取更多信息。
