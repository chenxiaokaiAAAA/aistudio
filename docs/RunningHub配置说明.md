# RunningHub 全能图片PRO-图生图 API 配置说明

## 📋 API 概述

RunningHub 全能图片PRO-图生图 API 是一个专业的图像编辑服务，支持4K超清画质输出。

**参考文档**: [RunningHub API 文档](https://www.runninghub.cn/call-api/api-detail/2004543527918551041?apiType=1)

## 🔍 与其他服务商的区别

### 与 nano-banana-edits 的区别

| 项目 | nano-banana-edits | RunningHub |
|------|-------------------|------------|
| **请求格式** | `multipart/form-data` | `application/json` |
| **图片参数** | 文件上传（`image` 或 `image[]`） | URL数组（`imageUrls`） |
| **接口路径** | `/v1/images/edits` | `/openapi/v2/rhart-image-n-pro/edit` |
| **图片数量** | 支持多图 | 最多10张 |
| **响应格式** | 可能同步或异步 | 异步（返回 `taskId`） |

### 主要特点

1. **请求格式**: 使用 `application/json`，不是 `multipart/form-data`
2. **图片参数**: 使用 `imageUrls` 数组传递图片URL（最多10张）
3. **响应格式**: 总是返回 `taskId`，需要异步轮询查询结果
4. **状态字段**: 使用 `status` 字段（QUEUED, RUNNING, SUCCEED, FAILED）

## 🔧 添加配置

### 方法1：使用脚本添加（推荐）

在项目根目录运行：

```bash
python scripts/add_runninghub_config.py
```

脚本会：
1. 检查是否已存在同名配置
2. 提示输入 API Key
3. 提示输入模型名称（可选）
4. 自动保存配置

### 方法2：通过管理后台添加

1. 登录管理后台
2. 访问 API 配置管理页面
3. 点击"添加配置"
4. 填写以下信息：

```
配置名称: RunningHub-全能图片PRO
API类型: runninghub-rhart-edit
国内Host: https://www.runninghub.cn
海外Host: https://www.runninghub.cn
API Key: [您的API Key]
绘画接口: /openapi/v2/rhart-image-n-pro/edit
结果接口: /openapi/v2/query (新格式，请求体只需要 taskId)
文件上传接口: /v1/file/upload
模型名称: rhart-image-n-pro (可选)
```

## 📝 配置字段说明

| 字段 | 值 | 说明 |
|------|-----|------|
| name | RunningHub-全能图片PRO | 配置名称 |
| api_type | runninghub-rhart-edit | API类型 |
| host_domestic | https://www.runninghub.cn | 国内访问地址 |
| host_overseas | https://www.runninghub.cn | 海外访问地址 |
| draw_endpoint | /openapi/v2/rhart-image-n-pro/edit | 图片编辑接口 |
| result_endpoint | /openapi/v2/query | 查询任务结果接口（新格式，请求体只需要 taskId） |
| is_sync_api | False | False=异步API（返回 taskId，需要轮询） |
| enable_retry | True | 启用重试机制 |
| is_active | True | 启用配置 |

## 🔄 请求格式

### 请求体示例

```json
{
  "prompt": "基于原图风格，将老奶奶替换为一只年迈慈祥的猴子奶奶...",
  "resolution": "1K",
  "aspectRatio": "3:4",
  "imageUrls": [
    "https://example.com/image1.jpg",
    "https://example.com/image2.jpg"
  ]
}
```

### 请求参数说明

- **prompt**: 提示词（必填）
- **resolution**: 分辨率，支持 `1K`, `2K`, `4K`（必填）
- **aspectRatio**: 图片比例，支持 `3:4`, `16:9`, `auto` 等（必填）
- **imageUrls**: 图片URL数组，最多10张（必填）

## 📥 响应格式

### 成功响应示例

```json
{
  "taskId": "2009204591912427521",
  "status": "QUEUED",
  "errorCode": "",
  "errorMessage": "",
  "results": null,
  "clientId": "",
  "promptTips": ""
}
```

### 响应字段说明

- **taskId**: 任务ID，用于查询结果
- **status**: 任务状态（QUEUED, RUNNING, SUCCEED, FAILED）
- **errorCode**: 错误码（如果有）
- **errorMessage**: 错误信息（如果有）
- **results**: 结果（初始为 null，完成后包含图片URL）

## 🔄 异步轮询

RunningHub API 是异步API，返回 `taskId` 后需要轮询查询结果：

1. **提交任务**: 调用 `/openapi/v2/rhart-image-n-pro/edit`，返回 `taskId`
2. **查询结果**: 使用 `taskId` 调用 `/openapi/v2/query` 查询结果（请求体: `{"taskId": "..."}`）
3. **状态检查**: 检查 `status` 字段，直到状态为 `SUCCEED` 或 `FAILED`

## ⚠️ 注意事项

1. **图片数量限制**: 最多支持10张图片，超过会自动截取前10张
2. **异步API**: RunningHub 总是返回 `taskId`，需要轮询查询结果
3. **图片URL**: 必须使用可访问的图片URL，不支持文件上传
4. **分辨率**: 支持 1K, 2K, 4K
5. **比例**: 支持 3:4, 16:9, auto 等

## 🧪 测试配置

配置添加后，可以通过以下方式测试：

1. **在管理后台查看配置**: 确认配置已正确保存
2. **创建测试任务**: 使用该配置创建一个测试任务
3. **查看日志**: 检查任务执行日志，确认 API 调用正常
4. **检查任务状态**: 确认 `taskId` 已正确保存，可以轮询查询结果

## 📚 相关文档

- [RunningHub API 文档](https://www.runninghub.cn/call-api/api-detail/2004543527918551041?apiType=1)
- [API服务商集成说明](../API服务商集成说明.md)
