# 检查文件存在和权限

## 🔍 问题

- ✅ 文件在根目录（从文件管理器看到）
- ❌ 手动启动时提示找不到文件

**可能原因**：
1. 文件权限问题
2. 路径问题
3. 文件确实存在但需要检查

---

## 🔧 立即检查

### 步骤1：确认文件存在

```bash
# 检查文件
ls -la /root/project_code/start_production.py
ls -la /root/project_code/test_server.py
ls -la /root/project_code/gunicorn.conf.py
```

### 步骤2：检查文件权限

```bash
# 检查权限
ls -lh /root/project_code/*.py | head -10

# 设置执行权限
chmod +x /root/project_code/start_production.py
```

### 步骤3：测试启动

```bash
# 确认当前目录
pwd

# 确认文件存在
ls -la start_production.py

# 测试启动
python start_production.py
```

---

## 📋 完整检查和修复脚本

```bash
#!/bin/bash
echo "=========================================="
echo "检查文件并修复"
echo "=========================================="
echo ""

# 1. 检查文件
echo "[1/4] 检查关键文件..."
cd /root/project_code
ls -la start_production.py test_server.py gunicorn.conf.py 2>/dev/null || echo "⚠️ 某些文件不存在"

echo ""
# 2. 检查权限
echo "[2/4] 检查并设置权限..."
chmod +x /root/project_code/start_production.py
chmod 644 /root/project_code/test_server.py 2>/dev/null || true
chmod 644 /root/project_code/gunicorn.conf.py 2>/dev/null || true
echo "✅ 权限已设置"

echo ""
# 3. 确认文件路径
echo "[3/4] 确认文件路径..."
echo "当前目录: $(pwd)"
echo "start_production.py 绝对路径: $(readlink -f start_production.py 2>/dev/null || echo $(pwd)/start_production.py)"
echo "文件存在: $(test -f start_production.py && echo '是' || echo '否')"

echo ""
# 4. 测试启动
echo "[4/4] 测试启动..."
source venv/bin/activate
python start_production.py
```

---

## 🎯 立即执行

在服务器上执行：

```bash
cd /root/project_code
ls -la start_production.py
chmod +x start_production.py
source venv/bin/activate
python start_production.py
```

如果文件确实存在，手动启动应该能运行。如果还有错误，把错误信息发给我。

---

## 🔧 如果手动启动成功

如果手动启动成功，说明文件没问题，可能是服务配置问题。需要检查服务配置中的路径。

---

## 📝 请执行

先执行简单的检查：

```bash
cd /root/project_code
ls -la start_production.py
pwd
python start_production.py
```

把结果发给我。
