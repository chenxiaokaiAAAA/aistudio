# 查看服务启动失败错误

## 🔍 问题

- ✅ 依赖已安装
- ❌ 服务启动失败：`status=2`（退出状态码2）

---

## 🔧 立即排查

### 步骤1：查看详细错误日志

```bash
# 查看服务日志
journalctl -u aistudio -n 100 --no-pager

# 查看应用错误日志
tail -50 /root/project_code/logs/error.log 2>/dev/null || echo "error.log不存在"
tail -50 /root/project_code/logs/startup.log 2>/dev/null || echo "startup.log不存在"
```

### 步骤2：手动测试启动（查看实时错误）

```bash
# 停止服务
systemctl stop aistudio

# 手动启动（会显示详细错误）
cd /root/project_code
source venv/bin/activate
python start_production.py
```

---

## 📋 完整排查命令

```bash
echo "=== 1. 服务日志（最近50行） ==="
journalctl -u aistudio -n 50 --no-pager

echo ""
echo "=== 2. 应用错误日志 ==="
tail -50 /root/project_code/logs/error.log 2>/dev/null || echo "error.log不存在"
tail -50 /root/project_code/logs/startup.log 2>/dev/null || echo "startup.log不存在"

echo ""
echo "=== 3. 检查文件 ==="
ls -la /root/project_code/start_production.py
ls -la /root/project_code/test_server.py
ls -la /root/project_code/gunicorn.conf.py

echo ""
echo "=== 4. 检查数据库 ==="
ls -la /root/project_code/instance/pet_painting.db 2>/dev/null || echo "数据库文件不存在"

echo ""
echo "=== 5. 手动测试启动（会显示详细错误）==="
echo "执行: systemctl stop aistudio"
echo "然后: cd /root/project_code && source venv/bin/activate && python start_production.py"
```

---

## 🎯 立即执行

先执行上面的**完整排查命令**，特别是查看服务日志：

```bash
journalctl -u aistudio -n 100 --no-pager
```

**或者手动测试启动**（会显示实时错误）：

```bash
systemctl stop aistudio
cd /root/project_code
source venv/bin/activate
python start_production.py
```

把错误信息发给我，我会根据具体错误提供解决方案。
