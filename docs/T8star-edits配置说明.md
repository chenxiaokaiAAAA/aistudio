# T8star-edits API 配置说明

## 📋 快速添加配置

### 方法1：直接运行脚本（推荐）

在项目根目录运行：

```bash
python scripts/add_t8star_edits_config.py
```

脚本会：
1. 检查是否已存在同名配置
2. 提示输入 API Key
3. 提示输入模型名称（可选）
4. 自动保存配置

### 方法2：通过管理后台添加

1. 登录管理后台
2. 访问 API 配置管理页面
3. 点击"添加配置"
4. 填写以下信息：

```
配置名称: T8star-edits
API类型: nano-banana-edits
国内Host: https://ai.t8star.cn
海外Host: https://ai.t8star.cn
API Key: [您的API Key]
绘画接口: /v1/images/edits
结果接口: /v1/images/tasks/{task_id}
文件上传接口: /v1/file/upload
模型名称: nano-banana-edits (可选)
```

## 🔧 配置字段说明

| 字段 | 值 | 说明 |
|------|-----|------|
| name | T8star-edits | 配置名称 |
| api_type | nano-banana-edits | API类型 |
| host_domestic | https://ai.t8star.cn | 国内访问地址 |
| host_overseas | https://ai.t8star.cn | 海外访问地址 |
| draw_endpoint | /v1/images/edits | 图片编辑接口 |
| result_endpoint | /v1/images/tasks/{task_id} | 查询任务结果接口 |
| file_upload_endpoint | /v1/file/upload | 文件上传接口 |
| is_sync_api | False | False=异步API（需要轮询查询结果） |
| enable_retry | True | 启用重试机制 |
| is_active | True | 启用配置 |

## 🧪 测试配置

配置添加后，可以通过以下方式测试：

1. **在管理后台查看配置**：确认配置已正确保存
2. **创建测试任务**：使用该配置创建一个测试任务
3. **查看日志**：检查任务执行日志，确认 API 调用正常

## ⚠️ 注意事项

1. **异步API**：T8Star 的 nano-banana-edits 是异步API，需要轮询查询结果
2. **代理配置**：T8Star 通常需要代理才能访问，确保代理配置正确
3. **API Key**：确保 API Key 正确，否则无法调用 API

## 📝 相关文档

- [API服务商集成说明](../API服务商集成说明.md)
