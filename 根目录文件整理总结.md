# 根目录文件整理总结

## ✅ 已整理完成

### 1. 文件分类整理
- ✅ 部署文档 → `docs/deployment/`
- ✅ 部署脚本 → `scripts/deployment/`
- ✅ 数据库脚本 → `scripts/database/`
- ✅ 工具脚本 → `scripts/tools/`
- ✅ 功能文档 → `docs/features/`

### 2. 根目录保留的文件

#### 核心运行文件（必需）
- `test_server.py` - 主应用
- `start.py`, `start_production.py` - 启动脚本
- `order_notification.py`, `wechat_notification.py` - 通知模块
- `printer_client.py`, `local_printer_client.py`, `local_printer.py` - 打印模块
- `gunicorn.conf.py`, `requirements.txt` - 配置文件
- `server_config.py`, `printer_config.py`, `size_config.py` - 配置

#### 重要文档（必需）
- `README.md` - 项目说明
- `根目录文件说明.md` - 文件结构说明
- `Linux命令快速参考.md` - 命令速查
- `README_重要文档.md` - 文档索引

## 🗑️ 可以清理的文件

### 历史遗留的二维码图片（可删除）

这些图片文件**不再使用**，系统已改为动态生成二维码：

- ❌ `merchant_6_qrcode.png` - 未在代码中使用
- ❌ `merchant_7_qrcode.png` - 未在代码中使用
- ❌ `website_qrcode.png` - 未在代码中使用
- ❌ `test_qrcode.jpg` - 未在代码中使用
- ❌ `qrcode_page_.jpg` - 未在代码中使用

**原因：**
- 系统使用 `qrcode` 库动态生成二维码
- 路由：`/merchant/qrcode.png` 动态生成
- 这些静态文件是历史遗留

**清理方法：**
```cmd
scripts\tools\清理根目录无用文件.bat
```

### 备份文件（可删除）

- ❌ `franchisee_routes.bak` - 备份文件，已不需要

### 被.gitignore忽略的文件（不影响）

这些文件会被Git忽略，但可以保留在本地：
- 图片文件（`*.png`, `*.jpg`）
- 数据库文件（`*.db`）
- 日志文件（`*.log`）

## 📋 进一步整理建议

### 可以移动到对应目录的文件

1. **API文档** → `docs/api/`
   - `API服务商集成说明.md`
   - `API模板管理模块说明.md`

2. **打印服务相关** → `打印-门店端-服务端/`
   - `README_打印代理服务.md`
   - `print_proxy_server.py`（如果还在根目录）
   - `start_print_proxy.bat`

3. **工具脚本** → `scripts/tools/`
   - `完整同步到GitHub并部署.bat`
   - `整理根目录文件.bat`
   - `photo_selection_detail.html`

## 🎯 最终根目录结构

整理完成后，根目录应该只有：

```
aistudio/
├── test_server.py              # 主应用
├── start.py                    # 开发启动
├── start_production.py         # 生产启动
├── gunicorn.conf.py            # Gunicorn配置
├── requirements.txt            # 依赖列表
├── server_config.py            # 服务器配置
├── printer_config.py          # 打印配置
├── size_config.py             # 尺寸配置
│
├── order_notification.py      # 订单通知
├── wechat_notification.py     # 微信通知
├── printer_client.py          # 打印客户端
├── local_printer_client.py    # 本地打印客户端
├── local_printer.py           # 本地打印模块
│
├── README.md                  # 项目说明
├── 根目录文件说明.md          # 文件结构说明
├── Linux命令快速参考.md       # 命令速查
├── README_重要文档.md         # 文档索引
│
├── app/                       # 应用目录
├── templates/                 # 模板目录
├── static/                    # 静态文件
├── config/                    # 配置文件
├── docs/                      # 文档目录
├── scripts/                   # 脚本目录
├── batch/                     # 批处理脚本
├── 打印-门店端-服务端/        # 打印服务
└── 小程序前端修改代码/         # 小程序前端
```

**约17个文件 + 9个目录**

## 🚀 快速操作

### 清理无用文件
```cmd
scripts\tools\清理根目录无用文件.bat
```

### 进一步整理
```cmd
scripts\tools\进一步整理根目录.bat
```

### 提交整理结果
```cmd
git add -A
git commit -m "整理：清理根目录无用文件，优化文件结构"
git push origin master
```
